---
// src/components/Skills.astro
import { CV_DATA } from '../data/cv-data.js';
const { skills } = CV_DATA;
---
<section id="habilidades">
    <h2 class="text-2xl mb-6">Habilidades Técnicas</h2>
    <div class="flex flex-wrap gap-2">
        {skills.map(skill => (
            <span class="py-1 px-3 rounded-md text-sm bg-sky-400/10 text-sky-300">
                {skill}
            </span>
        ))}
    </div>
</section>